<h2>Our top movies </h2>
<ul>
<% @movies.each do |movie| %>
<li><%= movie.title %></li>
<p><%= movie.overview %>
<div class="container">
<%= image_tag movie.poster_url, alt: movie.title, class: "movie-img" %>
</div>
<% end %>
</ul>
